{
  for (  final MeshPartMaterial mpm : node.meshPartMaterials)   mpm.meshPart.mesh.calculateBoundingBox(out,mpm.meshPart.indexOffset,mpm.meshPart.numVertices);
  for (  final Node child : node.children)   calculateBoundingBox(out,child);
}
