{
  if (updating)   throw new GdxRuntimeException("Cannot change animation during update");
  if (current == null)   current=anim;
 else {
    if (current.animation == anim.animation)     anim.time=current.time;
    animationPool.free(current);
    current=anim;
  }
  return anim;
}
