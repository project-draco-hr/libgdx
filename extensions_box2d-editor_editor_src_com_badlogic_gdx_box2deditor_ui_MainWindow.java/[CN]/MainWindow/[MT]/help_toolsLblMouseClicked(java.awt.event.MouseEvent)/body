{
  JOptionPane.showMessageDialog(this,"<html>" + "Edition tools can only be used when you defined at least one shape for the current asset.<br/><br/>" + ""+ "&bull; <u>Insert points</u>: select two or more adjacent points to insert new point(s) between them.<br/>"+ "&bull; <u>Remove points</u>: removes the selected points. Be careful because you can end up with shapes<br/>"+ "with less than 3 vertices.<br/>"+ "&bull; <u>Clear all points</u>: removes every shape from the current asset.");
}
