{
  if (primitiveType == GL10.GL_POINTS)   ensureIndices(4 * numRectangles);
 else   if (primitiveType == GL10.GL_LINES)   ensureIndices(8 * numRectangles);
 else   ensureIndices(6 * numRectangles);
}
