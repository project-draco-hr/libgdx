{
  for (int word=bits.length - 1; word >= 0; --word) {
    if (bits[word] != 0) {
      for (int bit=63; bit >= 0; --bit) {
        if ((bits[word] & (1L << (bit & 0x3F))) != 0L) {
          return (word << 6) + bit;
        }
      }
    }
  }
  return 0;
}
