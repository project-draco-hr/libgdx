{
  if (this.isVisible != visible) {
    this.isVisible=visible;
    if (this.isVisible)     onResume();
 else     onPause();
  }
 else {
    if (DEBUG)     Log.d(TAG," > visible state is current, skipping visibilityChanged event!");
  }
}
