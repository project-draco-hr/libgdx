{
  if (AndroidLiveWallpaperService.DEBUG)   Log.d(AndroidLiveWallpaperService.this.TAG," > onSurfaceChanged() " + isPreview() + " "+ hashCode()+ ", running: "+ runningEngines);
  super.onSurfaceChanged(holder,format,width,height);
}
