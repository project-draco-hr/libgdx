{
  InputStream is=null;
  try {
    is=file.read();
    return parse(is);
  }
 catch (  Exception ex) {
    throw new SerializationException("Error parsing file: " + file,ex);
  }
 finally {
    StreamUtils.closeQuietly(is);
  }
}
