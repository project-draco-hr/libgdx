{
  tmpVec.set(x,y,z).sub(position).nor();
  if (!tmpVec.isZero()) {
    float dot=tmpVec.dot(up);
    if (dot == 1) {
      up.set(direction).scl(-1);
    }
 else     if (dot == -1) {
      up.set(direction);
    }
    direction.set(tmpVec);
    normalizeUp();
  }
}
