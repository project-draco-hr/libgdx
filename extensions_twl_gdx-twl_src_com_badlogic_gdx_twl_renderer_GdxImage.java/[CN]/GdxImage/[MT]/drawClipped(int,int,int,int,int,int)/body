{
  while (repeatCountY-- > 0) {
    int y1=y + height;
    int x0=x;
    int cx=repeatCountX;
    while (cx-- > 0) {
      int x1=x0 + width;
      sprite.setBounds(x0,y,x1,y1);
      sprite.draw(renderer.batch);
      x0=x1;
    }
    y=y1;
  }
}
