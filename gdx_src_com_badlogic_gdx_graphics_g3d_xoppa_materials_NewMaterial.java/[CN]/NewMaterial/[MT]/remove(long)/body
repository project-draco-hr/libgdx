{
  for (int i=0; i < attributes.size; i++) {
    final long id=attributes.get(i).getID();
    if ((mask & id) == id) {
      attributes.removeIndex(i);
      disable(id);
      sorted=false;
    }
  }
}
