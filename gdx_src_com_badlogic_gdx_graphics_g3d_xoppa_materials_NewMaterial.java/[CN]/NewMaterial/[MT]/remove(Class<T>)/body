{
  final long id=getAttributeID(type);
  if (has(id)) {
    for (int i=0; i < attributes.size; i++)     if (attributes.get(i).getID() == id) {
      attributes.removeIndex(i);
      break;
    }
    sorted=false;
    disable(id);
  }
}
