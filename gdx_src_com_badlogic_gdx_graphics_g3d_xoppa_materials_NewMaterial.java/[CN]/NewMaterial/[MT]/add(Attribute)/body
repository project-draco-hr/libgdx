{
  for (int i=0; i < attributes.length; i++) {
    final Attribute attr=attributes[i];
    if (!has(attr.type)) {
      enable(attr.type);
      this.attributes.add(attr);
      sorted=false;
    }
  }
}
