{
  if (complete)   return true;
  complete=true;
  Array<Action> actions=this.actions;
  for (int i=0, n=actions.size; i < n; i++)   if (!actions.get(i).act(delta))   complete=false;
  return complete;
}
