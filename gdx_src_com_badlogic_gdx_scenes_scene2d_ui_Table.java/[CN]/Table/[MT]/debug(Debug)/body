{
  this.debug=debug;
  if (debug == Debug.none) {
    if (debugRects != null) {
      StageDebugRenderer.debugRectPool.freeAll(debugRects);
      debugRects.clear();
    }
  }
 else   invalidate();
  return this;
}
