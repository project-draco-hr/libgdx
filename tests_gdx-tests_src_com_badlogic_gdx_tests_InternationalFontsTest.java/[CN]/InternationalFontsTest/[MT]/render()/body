{
  Gdx.gl.glClear(GL10.GL_COLOR_BUFFER_BIT);
  batch.setProjectionMatrix(cam.combined);
  batch.begin();
  koreanFont.draw(batch,"?????????/?????????",0,22);
  cyrillicFont.draw(batch,"cyrillic text",0,44);
  thaiFont.draw(batch,"???????????????????????????",0,66);
  batch.end();
}
