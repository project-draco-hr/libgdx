{
  Set<String> keySet=parameters.keySet();
  String convertedParameters="";
  for (  String name : keySet)   convertedParameters+=name + "=" + parameters.get(name)+ "&";
  return convertedParameters;
}
