{
  for (int i=0; i < level; i++)   buffer.append(' ');
  buffer.append(actor);
  buffer.append('\n');
  if (actor instanceof Group) {
    Array<Actor> actors=((Group)actor).getActors();
    for (int i=0, n=actors.size; i < n; i++)     graphToString(buffer,actors.get(i),level + 1);
  }
}
