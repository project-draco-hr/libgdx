{
  if (position == size) {
    DynamicTreeNode[] old=stack;
    stack=new DynamicTreeNode[size * 2];
    size=stack.length;
    System.arraycopy(old,0,stack,0,old.length);
  }
  stack[position++]=i;
}
