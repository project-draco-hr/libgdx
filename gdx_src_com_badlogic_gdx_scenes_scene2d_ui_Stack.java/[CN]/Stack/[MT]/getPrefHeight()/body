{
  float height=0;
  Array<Actor> children=getChildren();
  for (int i=0, n=children.size; i < n; i++)   height=Math.max(height,LibgdxToolkit.instance.getPrefHeight(children.get(i)));
  return height * getScaleY();
}
