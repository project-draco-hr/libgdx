{
  float height=0;
  Array<Actor> children=getActors();
  for (int i=0, n=children.size; i < n; i++)   height=Math.max(height,LibgdxToolkit.instance.getMinHeight(children.get(i)));
  return height * getScaleY();
}
