{
  float width=0;
  Array<Actor> children=getChildren();
  for (int i=0, n=children.size; i < n; i++)   width=Math.max(width,LibgdxToolkit.instance.getPrefWidth(children.get(i)));
  return width * getScaleX();
}
