{
  float width=0;
  Array<Actor> children=getActors();
  for (int i=0, n=children.size; i < n; i++)   width=Math.max(width,LibgdxToolkit.instance.getMinWidth(children.get(i)));
  return width * getScaleX();
}
