{
  if (dirty == false)   bounds.inf();
  dirty=true;
  int offset=currentVertex * 3;
  vertices[offset]=x;
  vertices[offset + 1]=y;
  vertices[offset + 2]=z;
  currentVertex++;
  bounds.ext(x,y,z);
}
