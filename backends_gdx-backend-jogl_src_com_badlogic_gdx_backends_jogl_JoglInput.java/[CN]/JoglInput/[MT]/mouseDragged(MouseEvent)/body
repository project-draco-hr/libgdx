{
synchronized (this) {
    TouchEvent event=freeTouchEvents.newObject();
    event.pointer=0;
    event.x=e.getX();
    event.y=e.getY();
    event.type=TouchEvent.TOUCH_DRAGGED;
    touchEvents.add(event);
    touchX=event.x;
    touchY=event.y;
    touchDown=true;
  }
}
