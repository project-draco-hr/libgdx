{
  if (polygon.size == 0) {
    return;
  }
  while (step > 0) {
    polygon.insert(0,polygon.pop());
    polygon.insert(0,polygon.pop());
    --step;
  }
  while (step < 0) {
    polygon.add(polygon.removeIndex(0));
    ++step;
  }
  triangulate();
}
