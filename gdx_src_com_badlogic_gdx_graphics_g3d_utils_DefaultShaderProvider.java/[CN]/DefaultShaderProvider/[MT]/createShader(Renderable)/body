{
  Gdx.app.log("DefaultShaderProvider","Creating new shader");
  if (Gdx.graphics.isGL20Available())   return new TestShader(renderable.material,renderable.lights == null ? -1 : maxLightsCount);
  return new GLES10Shader(maxLightsCount);
}
