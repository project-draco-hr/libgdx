{
  Label label=((Label)ui.findActor("label"));
  if (character == '\b') {
    if (label.text.length() > "text input: ".length())     label.text=label.text.substring(0,label.text.length() - 1);
  }
 else {
    label.text+=character;
  }
  return false;
}
