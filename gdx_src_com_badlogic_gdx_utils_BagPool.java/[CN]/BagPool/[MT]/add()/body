{
  if (size == items.length) {
    resize((int)(size * 1.75f));
    T item=newObject();
    items[size++]=item;
    return item;
  }
  T item=(T)items[size];
  if (item == null)   item=newObject();
  items[size++]=item;
  return item;
}
