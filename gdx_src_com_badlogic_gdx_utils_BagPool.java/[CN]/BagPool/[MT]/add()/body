{
  if (size == items.length) {
    resize((int)(size * 1.75f),false);
    T item=newObject();
    items[size++]=item;
    return item;
  }
  T item=items[size];
  if (item == null)   item=newObject();
  items[size++]=item;
  return item;
}
