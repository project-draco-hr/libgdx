{
  Entry prev=table[index];
  Entry e=prev;
  while (e != null) {
    Entry next=e.next;
    if (e == entry) {
      size--;
      if (prev == e)       table[index]=next;
 else       prev.next=next;
      return;
    }
    prev=e;
    e=next;
  }
}
