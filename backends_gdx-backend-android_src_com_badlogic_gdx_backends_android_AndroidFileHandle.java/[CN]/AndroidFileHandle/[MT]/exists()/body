{
  if (type == FileType.Internal) {
    try {
      InputStream in=assets.open(file.getPath());
      in.close();
      return true;
    }
 catch (    Exception ex) {
      return false;
    }
  }
  return super.exists();
}
