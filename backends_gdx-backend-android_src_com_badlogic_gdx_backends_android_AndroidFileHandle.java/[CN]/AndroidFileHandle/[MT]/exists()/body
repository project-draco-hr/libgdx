{
  if (type == FileType.Internal) {
    String fileName=file.getPath();
    try {
      assets.open(fileName).close();
      return true;
    }
 catch (    Exception ex) {
      return false;
    }
  }
  return super.exists();
}
