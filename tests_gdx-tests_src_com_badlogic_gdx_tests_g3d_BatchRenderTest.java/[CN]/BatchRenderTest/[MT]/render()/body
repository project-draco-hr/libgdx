{
  final float delta=Gdx.graphics.getDeltaTime();
  if ((dbgTimer+=delta) >= 1f) {
    dbgTimer-=1f;
  }
  for (  final PointLight light : lights.pointLights)   light.position.rotate(Vector3.Y,delta * 45f);
  Gdx.gl.glViewport(0,0,Gdx.graphics.getWidth(),Gdx.graphics.getHeight());
  Gdx.gl.glClearColor(0,0,0,0);
  Gdx.gl.glClear(GL10.GL_COLOR_BUFFER_BIT | GL10.GL_DEPTH_BUFFER_BIT);
  Gdx.gl.glDisable(GL20.GL_CULL_FACE);
  renderBatch.begin(cam);
  for (int i=0; i < instances.size; i++) {
    if (instances.get(i).model == null)     Gdx.app.log("Test","Model " + i + " is null");
 else     renderBatch.render(instances.get(i),lights);
  }
  renderBatch.end();
}
