{
  if (cache == null)   cache=new GdxFontCache();
  BitmapFontCache bitmapCache=((GdxFontCache)cache).bitmapCache;
  bitmapFont.cacheMultiLineText(bitmapCache,str,0,yOffset,com.badlogic.gdx.graphics.Color.WHITE,width,HAlignment.values()[align.ordinal()]);
  return cache;
}
