{
  super.onPause();
  if (isFinishing()) {
    graphics.disposeRenderListener();
    graphics.clearManagedCaches();
  }
  if (graphics != null && graphics.view != null)   graphics.view.onPause();
  if (audio != null)   audio.pause();
  if (listener != null)   listener.pause();
}
