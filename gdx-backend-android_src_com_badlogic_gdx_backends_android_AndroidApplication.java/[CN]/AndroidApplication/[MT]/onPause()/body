{
  super.onPause();
  if (isFinishing())   graphics.disposeRenderListener();
  if (graphics.view != null)   graphics.view.onPause();
  audio.pause();
  if (listener != null)   listener.pause(this);
}
