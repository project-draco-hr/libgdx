{
  List<Vector2> mp=AppContext.instance().mousePath;
  for (int i=1; i < mp.size(); i++)   drawLine(mp.get(i),mp.get(i - 1),MOUSEPATH_COLOR,1);
  if (mp.size() > 1)   drawLine(mp.get(0),mp.get(mp.size() - 1),MOUSEPATH_COLOR,1);
}
