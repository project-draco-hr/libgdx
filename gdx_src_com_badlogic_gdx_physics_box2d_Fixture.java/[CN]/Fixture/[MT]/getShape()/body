{
  if (shape == null) {
    long shapeAddr=jniGetShape(addr);
    int type=Shape.jniGetType(shapeAddr);
    if (type == 0)     shape=new CircleShape(shapeAddr);
 else     shape=new PolygonShape(shapeAddr);
  }
  return shape;
}
