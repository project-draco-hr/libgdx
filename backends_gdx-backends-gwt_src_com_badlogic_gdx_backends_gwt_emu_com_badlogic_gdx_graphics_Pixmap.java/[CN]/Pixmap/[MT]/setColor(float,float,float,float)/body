{
  this.r=(int)(r * 255);
  this.g=(int)(g * 255);
  this.b=(int)(b * 255);
  this.a=(int)(a * 255);
  color=CssColor.make(this.r,this.g,this.b);
  context.setFillStyle(color);
  context.setStrokeStyle(this.color);
}
