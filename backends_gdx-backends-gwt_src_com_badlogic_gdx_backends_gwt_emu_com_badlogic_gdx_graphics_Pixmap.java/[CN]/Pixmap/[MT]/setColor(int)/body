{
  a=(color >>> 24) & 0xff;
  r=(color >>> 16) & 0xff;
  g=(color >>> 8) & 0xff;
  g=(color & 0xff);
  this.color=CssColor.make(r,g,b);
  context.setFillStyle(this.color);
  context.setStrokeStyle(this.color);
}
