{
  GdxTest test=new ModelLoaderTest();
  LwjglApplicationConfiguration config=new LwjglApplicationConfiguration();
  config.useGL20=test.needsGL20();
  config.width=480;
  config.height=320;
  new LwjglApplication(test,config);
}
