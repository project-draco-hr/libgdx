{
  if (index >= actions.size)   return true;
  if (actions.get(index).act(delta)) {
    if (actor == null)     return true;
    index++;
    if (index >= actions.size)     return true;
  }
  return false;
}
