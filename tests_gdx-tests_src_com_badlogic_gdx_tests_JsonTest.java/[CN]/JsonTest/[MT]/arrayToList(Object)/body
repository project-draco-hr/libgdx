{
  if (array == null || !array.getClass().isArray())   return array;
  ArrayList list=new ArrayList(java.lang.reflect.Array.getLength(array));
  for (int i=0, n=java.lang.reflect.Array.getLength(array); i < n; i++)   list.add(arrayToList(java.lang.reflect.Array.get(array,i)));
  return list;
}
