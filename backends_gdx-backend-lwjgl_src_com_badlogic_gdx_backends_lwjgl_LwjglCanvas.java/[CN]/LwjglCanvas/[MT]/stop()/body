{
  if (!running)   return;
  running=false;
  try {
    Display.destroy();
  }
 catch (  Throwable ignored) {
  }
  EventQueue.invokeLater(new Runnable(){
    public void run(){
      listener.pause();
      listener.dispose();
    }
  }
);
}
