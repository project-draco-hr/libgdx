{
  EventQueue.invokeLater(new Runnable(){
    public void run(){
      if (!running)       return;
      running=false;
      listener.pause();
      listener.dispose();
      Display.destroy();
    }
  }
);
}
