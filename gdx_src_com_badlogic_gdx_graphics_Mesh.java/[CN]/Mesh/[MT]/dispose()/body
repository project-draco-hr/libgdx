{
  meshes.remove(this);
  if (Gdx.graphics.isGL11Available() == false && Gdx.graphics.isGL20Available() == false)   return;
  if (Gdx.graphics.isGL20Available())   dispose(Gdx.graphics.getGL20());
 else   dispose(Gdx.graphics.getGL11());
}
