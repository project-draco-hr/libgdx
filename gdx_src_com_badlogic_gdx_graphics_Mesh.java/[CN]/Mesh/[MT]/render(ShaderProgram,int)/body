{
  if (!graphics.isGL20Available())   throw new IllegalStateException("can't use this render method with OpenGL ES 1.x");
  checkManagedAndDirty();
  render(shader,primitiveType,0,maxIndices > 0 ? getNumIndices() : getNumVertices());
}
