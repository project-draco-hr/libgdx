{
  try {
    if (parent != null && !parent.exists()) {
      return false;
    }
    if (exists()) {
      return false;
    }
    LocalStorage.setItem(getCanonicalPath(),"{}");
    return true;
  }
 catch (  IOException e) {
    return false;
  }
}
