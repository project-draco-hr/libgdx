{
  File file;
  if (type == FileType.External)   file=new File(this.externalPath + fileName);
 else   if (type == FileType.Internal) {
    file=new File(fileName);
    if (FileHandle.class.getResourceAsStream("/" + fileName) == null && !file.exists())     throw new GdxRuntimeException("File not found: " + fileName + " ("+ type+ ")");
  }
  return new JoglFileHandle(new File(fileName),type);
}
