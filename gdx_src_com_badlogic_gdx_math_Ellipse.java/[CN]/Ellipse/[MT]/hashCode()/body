{
  final int prime=53;
  int result=1;
  result=prime * result + NumberUtils.floatToIntBits(this.height);
  result=prime * result + NumberUtils.floatToIntBits(this.width);
  result=prime * result + NumberUtils.floatToIntBits(this.x);
  result=prime * result + NumberUtils.floatToIntBits(this.y);
  return result;
}
