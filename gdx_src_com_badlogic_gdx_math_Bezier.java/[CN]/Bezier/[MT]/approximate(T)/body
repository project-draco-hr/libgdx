{
  T p1=points.get(0);
  T p2=points.get(points.size - 1);
  T p3=v;
  float l1=p1.dst(p2);
  float l2=p3.dst(p2);
  float l3=p3.dst(p1);
  float s=(l2 * l2 + l1 * l1 - l3 * l3) / (2 * l1);
  return MathUtils.clamp((l1 - s) / l1,0f,1f);
}
