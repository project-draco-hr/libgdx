{
  if (rotation != 0 || scaleX != 1 || scaleY != 1)   throw new GdxRuntimeException("Only unrotated and unscaled actors may use this method.");
  localCoords.x+=x;
  localCoords.y+=y;
  return localCoords;
}
