{
  for (int i=0; i < players.length; i++) {
    players[i].Dispose();
    players[i]=null;
  }
synchronized (sync) {
    soundCounter--;
    if (soundCounter == 0) {
      playThread=null;
    }
  }
}
