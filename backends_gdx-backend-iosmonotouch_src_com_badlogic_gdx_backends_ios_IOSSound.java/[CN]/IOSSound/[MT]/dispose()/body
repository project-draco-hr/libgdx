{
synchronized (sync) {
    soundCounter--;
    stop();
    for (int i=0; i < players.length; i++) {
      players[i].Dispose();
    }
    players=null;
    if (soundCounter == 0) {
      playThread=null;
    }
  }
}
