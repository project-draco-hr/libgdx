{
  for (  String key : animationRefs) {
    KeyframeAnimation anim=animations.get(key);
    if (anim.removeRef() == 0) {
      animations.remove(key);
    }
  }
  for (  Mesh m : target) {
    if (m != null) {
      m.dispose();
    }
  }
}
