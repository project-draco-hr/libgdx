{
switch (type) {
case Internal:
    if (file.exists())     return true;
case Classpath:
  return FileWrapper.class.getResource("/" + file.getPath().replace('\\','/')) != null;
}
return file().exists();
}
