{
  String texturePath;
  if (parameter == null)   texturePath=Gdx.files.internal(fileName).pathWithoutExtension() + ".png";
 else   texturePath=parameter.texturePath;
  Texture texture=manager.get(texturePath,Texture.class);
  return new Skin(resolve(fileName),texture);
}
