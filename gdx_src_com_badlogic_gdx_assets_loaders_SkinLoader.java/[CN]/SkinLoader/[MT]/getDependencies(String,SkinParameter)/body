{
  Array<AssetDescriptor> deps=new Array();
  if (parameter == null)   deps.add(new AssetDescriptor(Gdx.files.internal(fileName).nameWithoutExtension() + ".png",Texture.class));
 else   deps.add(new AssetDescriptor(parameter.texturePath,Texture.class));
  return deps;
}
