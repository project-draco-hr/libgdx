{
  Vector ab=s.b.tmp().sub(s.a);
  float t=ab.dot(p.tmp2().sub(s.a));
  if (t <= 0) {
    o.set(s.a);
  }
 else {
    float denom=ab.dot(ab);
    if (t >= denom) {
      o.set(s.b);
    }
 else {
      t=t / denom;
      o.set(s.a).add(ab.mul(t));
    }
  }
}
