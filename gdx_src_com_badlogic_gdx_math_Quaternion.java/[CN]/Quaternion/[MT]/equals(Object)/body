{
  if (this == obj) {
    return true;
  }
  if (obj == null) {
    return false;
  }
  if (!(obj instanceof Quaternion)) {
    return false;
  }
  Quaternion other=(Quaternion)obj;
  return (Float.floatToIntBits(w) == Float.floatToIntBits(other.w)) && (Float.floatToIntBits(x) == Float.floatToIntBits(other.x)) && (Float.floatToIntBits(y) == Float.floatToIntBits(other.y))&& (Float.floatToIntBits(z) == Float.floatToIntBits(other.z));
}
