{
  if (type == GL10.GL_FIXED) {
    convertFixedToFloatbuffer(pointer,texCoordBuffer,stride);
    gl.glTexCoordPointer(size,GL10.GL_FLOAT,stride,texCoordBuffer);
  }
 else   gl.glTexCoordPointer(size,type,stride,pointer);
}
