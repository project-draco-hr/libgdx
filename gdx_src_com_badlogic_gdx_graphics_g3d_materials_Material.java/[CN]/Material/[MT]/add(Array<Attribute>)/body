{
  for (int i=0; i < attributes.size; i++) {
    final Attribute attr=attributes.get(i);
    if (!has(attr.type)) {
      enable(attr.type);
      this.attributes.add(attr);
      sorted=false;
    }
  }
}
