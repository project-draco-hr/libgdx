{
  sizeInvalid=false;
  if (wrap)   bounds.set(cache.getFont().getWrappedBounds(text,getWidth()));
 else   bounds.set(cache.getFont().getMultiLineBounds(text));
  bounds.width*=fontScaleX;
  bounds.height*=fontScaleY;
}
