{
  int modeValue=0;
  if (mode == URLFilteringMode.Blacklist)   modeValue=1;
  if (mode == URLFilteringMode.Whitelist)   modeValue=2;
  Awesomium.INSTANCE.awe_WebView_setURLFilteringMode(webView,modeValue);
}
