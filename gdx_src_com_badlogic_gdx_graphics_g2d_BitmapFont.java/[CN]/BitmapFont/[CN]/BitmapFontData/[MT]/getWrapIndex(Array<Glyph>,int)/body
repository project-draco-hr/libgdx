{
  for (int i=start; i >= 1; i--) {
    char ch=(char)glyphs.get(i).id;
    if (isWhitespace(ch)) {
      while (--i >= 1)       if (!isWhitespace((char)glyphs.get(i).id))       break;
      return i + 1;
    }
    if (i < start && isBreakChar(ch))     return i;
  }
  return 0;
}
