{
  List<Vector3> tangents=getTangents(numPoints);
  ArrayList<Vector3> normals=new ArrayList<Vector3>();
  int i=0;
  for (  Vector3 tangent : tangents)   normals.add(new Vector3(tangent).crs(up.get(i++)).nor());
  return normals;
}
