{
  List<Vector> tangents=getTangents(numPoints);
  ArrayList<Vector> normals=new ArrayList<Vector>();
  int i=0;
  for (  Vector tangent : tangents)   normals.add(new Vector(tangent).crs(up.get(i++)).nor());
  return normals;
}
