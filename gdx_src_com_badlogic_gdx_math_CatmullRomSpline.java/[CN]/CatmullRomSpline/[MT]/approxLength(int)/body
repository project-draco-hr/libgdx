{
  float tempLength=0;
  for (int i=0; i < samples; ++i) {
    tmp3.set(tmp4);
    valueAt(tmp4,((float)i) / ((float)samples - 1));
    if (i > 0)     tempLength+=tmp3.dst(tmp4);
  }
  return tempLength;
}
