{
  String[] libNames=null;
  if (is64Bit)   libNames=new String[]{"OpenAL64.dll","lwjgl64.dll"};
 else   libNames=new String[]{"OpenAL32.dll","lwjgl.dll"};
  for (  String libName : libNames)   loadLibrary(libName,"/native/windows/",System.getProperty("java.io.tmpdir") + File.separator);
}
