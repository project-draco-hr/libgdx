{
  for (int i=0; i < vmMethod.spec.length - 1; ++i) {
    if (vmMethod.spec[i] == ')') {
      return Class.forCanonicalName(vmMethod.class_.loader,new String(vmMethod.spec,i + 1,vmMethod.spec.length - i - 2,false));
    }
  }
  throw new RuntimeException();
}
