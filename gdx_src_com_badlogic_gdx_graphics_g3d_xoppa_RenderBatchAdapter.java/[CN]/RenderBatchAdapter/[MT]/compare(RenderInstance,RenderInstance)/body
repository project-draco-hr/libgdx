{
  final boolean b1=o1.material.isNeedBlending();
  final boolean b2=o2.material.isNeedBlending();
  if (b1 != b2)   return b1 ? 1 : -1;
  if (o1.shader == o2.shader)   return o1.shader.compare(o1,o2);
  return o1.shader.compareTo(o2.shader);
}
