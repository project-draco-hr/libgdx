{
  float spacing=this.spacing;
  float groupWidth=getWidth() > 0 ? getWidth() : getMinWidth();
  float y=reverse ? 0 : (getHeight() > 0 ? getHeight() : getMinHeight());
  float dir=reverse ? 1 : -1;
  SnapshotArray<Actor> children=getChildren();
  for (int i=0, n=children.size; i < n; i++) {
    Actor child=children.get(i);
    float width, height;
    if (child instanceof Layout) {
      Layout layout=(Layout)child;
      width=layout.getPrefWidth();
      height=layout.getPrefHeight();
      if (width == 0 || width > groupWidth) {
        width=groupWidth;
      }
    }
 else {
      width=child.getWidth();
      height=child.getHeight();
    }
    float x;
    if ((alignment & Align.left) != 0)     x=0;
 else     if ((alignment & Align.right) != 0)     x=groupWidth - width;
 else     x=(groupWidth - width) / 2;
    if (!reverse)     y+=(height + spacing) * dir;
    if (round)     child.setBounds(Math.round(x),Math.round(y),Math.round(width),Math.round(height));
 else     child.setBounds(x,y,width,height);
    if (reverse)     y+=(height + spacing) * dir;
  }
}
