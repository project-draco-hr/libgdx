{
  for (int i=0; i < model.meshes.length; i++) {
    MD5Mesh mesh=model.meshes[i];
    if (useJni)     mesh.calculateVerticesJni(skeleton,vertices[i]);
 else     mesh.calculateVertices(skeleton,vertices[i]);
  }
}
