{
  this.width=width;
  this.height=height;
  this.batch=batch;
  ownsBatch=false;
  root=new Group();
  root.setStage(this);
  camera=new OrthographicCamera();
  setViewport(width,height,stretch);
}
