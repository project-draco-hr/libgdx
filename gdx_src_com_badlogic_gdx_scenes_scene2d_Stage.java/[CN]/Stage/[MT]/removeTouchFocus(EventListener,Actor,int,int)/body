{
  SnapshotArray<TouchFocus> touchFocuses=this.touchFocuses;
  for (int i=touchFocuses.size - 1; i >= 0; i--) {
    TouchFocus focus=touchFocuses.get(i);
    if (focus.listener == listener && focus.actor == actor && focus.pointer == pointer && focus.button == button)     TouchFocus.pool.free(touchFocuses.removeIndex(i));
  }
}
