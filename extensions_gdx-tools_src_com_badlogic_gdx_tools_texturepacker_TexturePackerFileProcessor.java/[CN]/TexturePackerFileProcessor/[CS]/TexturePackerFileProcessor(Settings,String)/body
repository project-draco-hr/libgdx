{
  this.defaultSettings=defaultSettings;
  if (packFileName.indexOf('.') == -1 || packFileName.toLowerCase().endsWith(".png") || packFileName.toLowerCase().endsWith(".jpg")) {
    packFileName+=".atlas";
  }
  this.packFileName=packFileName;
  setFlattenOutput(true);
  addInputSuffix(".png",".jpg");
}
