{
  amplitude=application.newMesh(1024,false,false,false,false,0,false);
  thread=new Thread(new Runnable(){
    @Override public void run(){
      AudioDevice device=application.getAudioDevice();
      NativeMP3Decoder decoder=new NativeMP3Decoder("D:/music/Audioslave/Audioslave/Cochise.mp3");
      while (decoder.readSamples(samples) > 0 && !finished)       device.writeSamples(samples);
    }
  }
);
  thread.start();
}
