{
  SpriteDrawable drawable=new SpriteDrawable(this);
  Sprite sprite=drawable.getSprite();
  if (sprite instanceof AtlasSprite)   sprite=new AtlasSprite((AtlasSprite)sprite);
 else   sprite=new Sprite(sprite);
  sprite.setColor(tint);
  drawable.setSprite(sprite);
  return drawable;
}
