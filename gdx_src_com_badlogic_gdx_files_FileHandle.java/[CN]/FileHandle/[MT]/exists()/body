{
switch (type) {
case Internal:
    if (file.exists())     return true;
case Classpath:
  return FileHandle.class.getResourceAsStream("/" + file.getPath().replace('\\','/')) != null;
}
return file().exists();
}
