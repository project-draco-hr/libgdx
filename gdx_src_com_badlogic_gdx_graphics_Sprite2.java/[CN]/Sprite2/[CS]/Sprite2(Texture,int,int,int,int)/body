{
  this.texture=texture;
  setTextureRegion(srcX,srcY,srcWidth,srcHeight);
  setColor(1,1,1,1);
  width=srcWidth;
  height=srcHeight;
  originX=width / 2;
  originY=height / 2;
  scaleX=1;
  scaleY=1;
  rotation=0;
  dirty=true;
}
