{
  InputStream in=files.readInternalFile(filename);
  JoglFont font=new JoglFont(this,in,size,style,managed);
  try {
    in.close();
  }
 catch (  IOException e) {
    return null;
  }
  return font;
}
