{
  boolean added=false;
  snapshot();
  selected.clear();
  for (int i=0, n=items.size; i < n; i++) {
    T item=items.get(i);
    if (selected.add(item))     added=true;
  }
  if (added && programmaticChangeEvents && fireChangeEvent())   revert();
 else   lastSelected=items.peek();
  cleanup();
}
