{
  point.x+=actor.x;
  point.y+=actor.y;
  Actor parent=actor.parent;
  while (parent != null) {
    if (parent instanceof Group) {
      point.x+=parent.x;
      point.y+=parent.y;
    }
    parent=parent.parent;
  }
}
