{
  R.TaggedJointPos=new Vector3[num];
  for (int i=0; i < num; i++)   R.TaggedJointPos[i]=new Vector3();
  R.TaggedJoint=new Quaternion[num];
  for (int i=0; i < num; i++)   R.TaggedJoint[i]=new Quaternion(0,0,0,0);
}
