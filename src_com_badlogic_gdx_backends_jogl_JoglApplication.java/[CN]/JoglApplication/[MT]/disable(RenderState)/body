{
  GL gl=GLContext.getCurrent().getGL();
  if (state == RenderState.Blending)   gl.glDisable(GL.GL_BLEND);
  if (state == RenderState.DepthTest) {
    gl.glDisable(GL.GL_DEPTH_TEST);
    gl.glDepthMask(false);
  }
  if (state == RenderState.Lighting)   gl.glDisable(GL.GL_LIGHTING);
  if (state == RenderState.Texturing)   gl.glDisable(GL.GL_TEXTURE_2D);
  if (state == RenderState.Culling)   gl.glDisable(GL.GL_CULL_FACE);
  if (state == RenderState.AlphaTest)   gl.glDisable(GL.GL_ALPHA_TEST);
}
