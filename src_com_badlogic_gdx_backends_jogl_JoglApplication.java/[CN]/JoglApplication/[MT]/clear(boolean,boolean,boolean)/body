{
  GL gl=GLContext.getCurrent().getGL();
  int flags=(color ? GL.GL_COLOR_BUFFER_BIT : 0) | (depth ? GL.GL_DEPTH_BUFFER_BIT : 0) | (stencil ? GL.GL_STENCIL_BUFFER_BIT : 0);
  if (depth)   gl.glDepthMask(true);
  gl.glClear(flags);
}
