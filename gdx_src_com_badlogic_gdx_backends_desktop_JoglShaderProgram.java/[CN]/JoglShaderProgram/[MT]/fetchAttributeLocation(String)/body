{
  Integer location;
  if ((location=attributes.get(name)) == null) {
    location=gl.glGetAttribLocation(program,name);
    if (location == -1)     throw new IllegalArgumentException("no attribute with name '" + name + "' in shader");
    attributes.put(name,location);
  }
  return location;
}
