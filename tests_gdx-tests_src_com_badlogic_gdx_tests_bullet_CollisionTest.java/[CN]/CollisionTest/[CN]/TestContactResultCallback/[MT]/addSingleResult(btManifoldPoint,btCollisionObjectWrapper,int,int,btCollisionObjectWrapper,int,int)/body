{
  btCollisionObject other=colObj0Wrap.getM_collisionObject() == projectile.body ? colObj1Wrap.getM_collisionObject() : colObj0Wrap.getM_collisionObject();
  if (other != null && other.userData != null && other.userData instanceof BulletEntity) {
    if (!hits.contains((BulletEntity)other.userData,true))     hits.add((BulletEntity)other.userData);
  }
  return 0f;
}
