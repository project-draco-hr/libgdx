{
  try {
    if (tasks.size() == 0) {
      if (preloadQueue.size == 0)       return true;
      nextTask();
    }
    return updateTask() && preloadQueue.size == 0;
  }
 catch (  Throwable t) {
    return handleTaskError(t);
  }
}
