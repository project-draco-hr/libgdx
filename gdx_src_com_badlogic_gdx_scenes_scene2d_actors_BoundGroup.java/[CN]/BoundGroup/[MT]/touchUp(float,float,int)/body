{
  if (focusedActor != null) {
    return super.touchUp(x,y,pointer);
  }
 else {
    if (!(x > 0 && y > 0 && x < width && y < height))     return false;
    return super.touchUp(x,y,pointer);
  }
}
