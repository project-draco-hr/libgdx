{
  for (int i=0, n=regions.size(); i < n; i++) {
    AtlasRegion region=regions.get(i);
    if (region.name.equals(name)) {
      int[] splits=region.splits;
      if (splits == null)       throw new IllegalArgumentException("Region does not have ninepatch splits: " + name);
      return new NinePatch(region,splits[0],region.getRegionWidth() - splits[1],splits[2],region.getRegionHeight() - splits[3]);
    }
  }
  return null;
}
