{
  float dt=(msecs / 1000.0f) / iters;
  for (int i=0; i < iters; i++) {
    clearBallContacts();
    world.step(dt,10,10);
    processBallContacts();
  }
  gameTime+=msecs;
  processElementTicks();
  processScheduledActions();
  processGameMessages();
  getDelegate().tick(this,msecs);
}
