{
  Field field=null;
  try {
    field=oldInstance.getClass().getDeclaredField(pairs.get(oldInstance));
  }
 catch (  Exception e) {
    enc.getExceptionListener().exceptionThrown(e);
  }
  return new Expression(oldInstance,field,"get",new Object[]{oldInstance.getClass()});
}
