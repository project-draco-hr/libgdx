{
  if (mode == MatrixMode.ModelView)   gl.glMatrixMode(GL10.GL_MODELVIEW);
  if (mode == MatrixMode.Projection)   gl.glMatrixMode(GL10.GL_PROJECTION);
  if (mode == MatrixMode.Texture)   gl.glMatrixMode(GL10.GL_TEXTURE);
}
