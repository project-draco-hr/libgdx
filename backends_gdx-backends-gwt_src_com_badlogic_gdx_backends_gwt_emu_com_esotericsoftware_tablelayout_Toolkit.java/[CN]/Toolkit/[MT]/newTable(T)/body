{
  try {
    return (T)ReflectionCache.getType(parent.getClass()).newInstance();
  }
 catch (  Exception ex) {
    throw new RuntimeException("Unable to create an instance of: " + parent.getClass().getName());
  }
}
