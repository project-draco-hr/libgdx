{
  int n=src.length - offset;
  if (tempFloat.capacity() < n)   tempFloat=ByteBuffer.allocateDirect(n * 4).order(ByteOrder.nativeOrder()).asFloatBuffer();
 else   tempFloat.clear();
  tempFloat.put(src,offset,src.length - offset);
  tempFloat.flip();
  return tempFloat;
}
