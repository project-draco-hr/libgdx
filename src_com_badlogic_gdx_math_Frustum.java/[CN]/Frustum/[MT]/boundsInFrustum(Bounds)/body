{
  for (int i=0; i < planes.size(); i++) {
    int out=0;
    for (int j=0; j < bounds.getCorners().length; j++)     if (planes.get(i).testPoint(bounds.getCorners()[j]) == Intersection.Back)     out++;
    if (out == 8)     return false;
  }
  return true;
}
