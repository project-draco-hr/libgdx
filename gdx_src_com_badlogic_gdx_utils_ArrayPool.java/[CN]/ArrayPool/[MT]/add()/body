{
  T[] items=this.items;
  if (size == items.length) {
    T item=newObject();
    resize((int)(size * 1.75f),false)[size++]=item;
    return item;
  }
  T item=items[size];
  if (item == null)   item=newObject();
  items[size++]=item;
  return item;
}
