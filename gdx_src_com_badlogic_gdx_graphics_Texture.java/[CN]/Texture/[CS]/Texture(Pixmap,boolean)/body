{
  this.isManaged=false;
  this.isMipMap=mipmap;
  this.file=null;
  this.textureData=null;
  glHandle=createGLHandle();
  uploadImageData(pixmap);
  if (mipmap)   minFilter=TextureFilter.MipMap;
  setFilter(minFilter,magFilter);
  setWrap(uWrap,vWrap);
}
