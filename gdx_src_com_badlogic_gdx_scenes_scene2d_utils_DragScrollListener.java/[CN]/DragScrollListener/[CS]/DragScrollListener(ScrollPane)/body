{
  this.scroll=scroll;
  scrollUp=new Task(){
    public void run(){
      scroll.setScrollY(scroll.getScrollY() - 15);
    }
  }
;
  scrollDown=new Task(){
    public void run(){
      scroll.setScrollY(scroll.getScrollY() + 15);
    }
  }
;
}
