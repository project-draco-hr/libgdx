{
  if (!useHWMipMap) {
    generateMipMapCPU(pixmap,texture,disposePixmap);
    return;
  }
  if (Gdx.app.getType() == ApplicationType.Android) {
    if (Gdx.graphics.isGL20Available())     generateMipMapGLES20(pixmap,disposePixmap);
 else     generateMipMapCPU(pixmap,texture,disposePixmap);
  }
 else {
    generateMipMapDesktop(pixmap,texture,disposePixmap);
  }
}
