{
  setWriter(writer);
  try {
    writeValue(object,knownType,elementType);
  }
  finally {
    try {
      this.writer.close();
    }
 catch (    Exception ignored) {
    }
    this.writer=null;
  }
}
