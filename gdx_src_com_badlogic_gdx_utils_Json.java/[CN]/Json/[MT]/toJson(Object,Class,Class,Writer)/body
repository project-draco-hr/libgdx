{
  setWriter(writer);
  try {
    writeValue(object,knownType,elementType);
  }
  finally {
    try {
      this.writer.close();
    }
 catch (    IOException ignored) {
    }
    this.writer=null;
  }
}
