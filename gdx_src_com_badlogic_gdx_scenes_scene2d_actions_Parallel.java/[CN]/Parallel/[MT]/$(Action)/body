{
  Parallel action=pool.obtain();
  action.actions.clear();
  if (action.finished == null || action.finished.length < actions.length)   action.finished=new boolean[actions.length];
  int len=actions.length;
  for (int i=0; i < len; i++)   action.actions.add(actions[i]);
  return action;
}
